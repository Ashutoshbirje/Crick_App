# CMD

set NODE_OPTIONS=--openssl-legacy-provider
npm start

// issue 
// NO + 1 RUN + WICKET (RUN OUT)
// WICKET (RUN OUT) + 1 RUN
// Wicket not selected and back add extra ball to batsman account 
// Push half stack into recent overs
// disable bowler name after every over (refresh is req - avoid)
// disable wicket table after click on w button (refresh is req - avoid)

// Feature acpects 
// Real-time batsman / bawler in scorecard (New Feature) | Clear Cache of old state (recentover both inning, batsman of inning2) | User side - Show current bowler in second inning2 with green| Update real time (recentover) 
// Pointable (New Feature)
// Team Runrate (New Feature)
// States (New Feature)
// Single admin (New Feature)
// Registration for match + Payment (New Feature)
// Advertisement (New Feature)

// final / semifinal / live tag on the live scorecard (New Feature)
// Form to fill data on website (New Feature)
// Screen Orientation 
